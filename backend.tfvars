# Backend configuration variables for dynamic `terraform init`
# Usage: terraform init -backend-config=backend.tfvars
# Replace with your actual GCP Project ID:

bucket = "terraform-state-my-gcp-dev-project-id"
